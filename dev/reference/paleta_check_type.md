# Check if a colour palette is divergent, sequential, or qualitative

Check if a colour palette is divergent, sequential, or qualitative

## Usage

``` r
paleta_check_type(name, pal_type = c("sequential", "divergent", "qualitative"))
```
